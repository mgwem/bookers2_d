<%= form_with model:[book, book_comment], local: false do |f| %>
  <%= f.text_area :comment, rows: '5', placeholder: "コメントをここに", class: "w-100", id: "text" %>
  <%= f.submit "送信", class: "mt-20 pull-right" %>
<% end %>